// const router = require("express").Router();
// const ballotController = require("../../controller/ballotController");

// // Matches with "/api/books"
// router.route("/")
//     .get(ballotController.findAll)
//     .post(ballotController.create);

// // Matches with "/api/ballot/:id"
// router
//     .route("/:id")
//     .get(ballotController.findById)
//     .put(ballotController.update)
//     .delete(ballotController.remove);

// module.exports = router;
